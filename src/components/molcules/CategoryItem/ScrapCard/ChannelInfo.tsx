import { contentProps } from "../../../../types/ContentType";

function ChannelInfo(content: contentProps['content']) {
    const { channelImageUrl, channelName, publishedDate } = content;

    return (
        <div>

        </div>
    );
}

export default ChannelInfo;

import { Button } from "@mui/material";

function BoardPublishButton() {
    return (
        <Button
            variant="contained"
            size="large"
        >
            트렌딩 보드에 게시
        </Button>
    )
}

export default BoardPublishButton;

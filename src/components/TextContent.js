const TextContent = ({ textContent }) => {
    return (
        <div className="TextContent">
            <div className="textContentText">{textContent}</div>
        </div>
    );
};

export default TextContent;